import React from 'react';
import ComingSoon from '../../components/ComingSoon';

const HostSignupPage: React.FC = () => (
  <ComingSoon
    feature="Host signup"
    description="The standalone host signup flow will be stitched in here while we decouple dependencies from the legacy client."
  />
);

export default HostSignupPage;
