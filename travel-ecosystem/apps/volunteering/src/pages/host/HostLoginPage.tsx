import React from 'react';
import ComingSoon from '../../components/ComingSoon';

const HostLoginPage: React.FC = () => (
  <ComingSoon
    feature="Host authentication"
    description="Authentication screens for hosts will be migrated into this micro-frontend, maintaining parity with the legacy experience."
  />
);

export default HostLoginPage;
