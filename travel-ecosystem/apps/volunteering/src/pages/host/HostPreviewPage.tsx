import React from 'react';
import ComingSoon from '../../components/ComingSoon';

const HostPreviewPage: React.FC = () => (
  <ComingSoon
    feature="Host profile preview"
    description="The host preview dashboard will render here once the data fetching logic and UI widgets are extracted into this micro-frontend."
  />
);

export default HostPreviewPage;
